# Tenant Isolation & Platform Admin Remediation — Tracker

**Full plan:** `docs/tenant_isolation_and_platform_admin_plan.md`
**Policy record:** `docs/adr/tenant-isolation-and-shared-content.md`
**Inventory:** `docs/tenant-isolation/ownership-matrix.md`

## Freeze — lifted 2026-09-05

New tenant-owned **persistence** in Room Booking and Wayfinding/AI Wayfinding was frozen from 2026-09-05 until the P1–P3 exit gates passed. All three passed the same day (P3's migration applied and deployed), so the freeze is lifted — Room Booking/Wayfinding/AI Wayfinding tenant-owned development may resume normally. P4/P5a tests are still expected on every relevant resource per the plan's §6 execution order.

## Milestone status

Order per the plan's §6 (Recommended Execution Order):

- [x] **P0** — Isolation inventory, ADR, engineering rule, freeze (this doc + `ownership-matrix.md` + `.github/PULL_REQUEST_TEMPLATE.md`)
- [x] **P1** — Replace Members/invite mocks with the real tenant backend (`membersApi`, `DELETE /org/invites/:id`, `GET /org/invite/:token`, transactional accept-race fix, email normalization)
- [x] **P2** — Live membership/session validation for HTTP and WebSocket (`User.authVersion`, live `JwtStrategy`/`PlayerJwtStrategy` lookups, `ScreenGateway` live-checks, dashboard query-key/cache-clear boundary)
- [x] **P3** — Separate platform-shared library authority from tenant roles (migration applied and deployed 2026-09-05 — see below)
- [x] **P4** — Secure media, thumbnails, derived files, and screenshots (implemented 2026-09-05, deployed 2026-09-06 alongside P5a's PairingSession work below; tasks 9/10 explicitly deferred)
- [x] **P5a** — Harden tenant relations and database invariants (all four schema tasks done: AuditLog/DesignDraft fixes and PlatformAuditLog deployed 2026-09-05; `PairingSession` model + `Screen.organizationId NOT NULL` and the full composite-tenant-FK/trigger set across every relation on the plan's list deployed 2026-09-06 — see below)
- [~] **P6a** — Complete security-critical Super Admin controls (depends on P2, done) — first pass done and deployed 2026-09-06 (suspend-reason, cross-tenant member/invite management, session revocation, name edit, audit read endpoint); password reset, email-change-with-verification, slug change, and locale settings explicitly deferred — see below
- [x] **P7** — Make published templates immutable and safe to distribute (implemented and tested 2026-09-07; not yet deployed to production — see below)
- [x] **P6b** — Super Admin operational visibility (implemented and tested 2026-09-07; not yet deployed to production)
- [~] **P8** — Remove or isolate remaining browser-global production mocks (parallelizable with P3–P5a) — Audit Log + proof of play/reports done 2026-09-07, four features remain mock-backed
- [ ] **P9** — Tenant-isolation verification suite and rollout
- [ ] **P5b** — Optional PostgreSQL RLS and database-role separation (only if a compliance/threat-model decision requires it — see plan §7)

## P3 status — done

- `assets.controller.ts`'s three shared-library write routes moved from `@Roles('LIBRARY_MANAGER')` to method-level `SuperAdminGuard` + `@RequireSuperAdmin()`; `RolesGuard`'s `LIBRARY_MANAGER` carve-out removed.
- `LIBRARY_MANAGER` removed from `UserRole` in `schema.prisma`, the dashboard's `UserRole` type, the Members invite/role pickers, `usePermissions().canManageLibrary` (now `isSuperAdmin`-driven), and both locale message files. `db:audit-library-manager` found exactly one holder (a 2026-08-25 smoke-test fixture); migrated to `EDITOR` before the enum-value migration ran — see the resolved decision below.
- `Asset.sourceLibraryAssetId` added (self-relation to the library original) — `copyFromLibrary` sets it; `remove()`/`removeFromLibrary()` use it instead of a storage-key-match count to decide who may ever delete the shared object (task 7's race-prone inference).
- Shared-library storage prefix renamed `system/assets/...` → `platform/assets/...` (task 5) in `assets.service.ts` and `seed-library.ts`; no migration needed since the library had zero rows in this database.
- `PlatformAuditLog` model + `PlatformAuditService` added (task 9) and wired into the three library-mutation methods with before/after metadata — deliberately separate from `AuditLog`, whose required `organizationId` can't represent a no-target-tenant platform action.
- Verified, not changed: `Theme`/`PoiCategory` (task 8) already can't be written with `organizationId: null` through any tenant route, and their update/delete paths already reject `organizationId === null` rows — no live gap found there.
- Migration `20260905210752_p3_shared_library_authority` applied to the production database and `lumina-api-1`/`lumina-dashboard-1` rebuilt and redeployed, 2026-09-05.

Deferred, not attempted: task 4 (a delegated platform library-permission page — not required for the first release), and the `PlatformAuditLog` target-tenant column P5a/P6a will need once they migrate `PlatformTenantsService`/`EntitlementsService` off the cross-org `AuditLog.userId` misuse (plan §2.10).

## P4 status — done (tasks 9/10 explicitly deferred)

**Goal achieved:** every URL that used to embed a raw, permanent, unauthenticated storage key (`StorageService.publicUrl`) now resolves through `MediaController` by database identity (`assetId`/`binaryId`/`screenId`) and requires a short-lived signed `?token=` capability minted by the new `MediaTokenService` (`apps/api/src/modules/storage/media-token.service.ts`) — same manual-verify-not-a-guard pattern `PlaylistsService`'s preview-link token already used, since these routes are deliberately unauthenticated in the JwtAuthGuard sense (an `<img>`/`<video>` tag can't send a Bearer header): the token *is* the authorization decision, frozen at mint time by whichever caller already ran a real ownership check.

New routes (`assets/:id`, `assets/:id/thumbnail`, `assets/:id/pages/:n`, `binaries/:id`, `screens/:id/screenshot`), all in `media.controller.ts`. TTL policy (`storage.service.ts`'s `MEDIA_TOKEN_TTL_SEC`): screenshots 5 min, dashboard-facing asset/thumbnail links 1h, manifest-embedded asset/binary links 24h (long-bounded, not short — apps/player's media-download-manager.ts caches by immutable identity, not URL, but an in-flight download's own retries reuse the URL it started with, and a single attempt is allowed up to 4h; see the comment on `MEDIA_TOKEN_TTL_SEC` for the full reasoning). `Referrer-Policy: no-referrer` added to every response; cache headers changed from `public, max-age=31536000` to `private, max-age=3600` so a shared/CDN cache can't retain a capability URL (task 8).

Every one of the ~20 `storage.publicUrl(...)` call sites across `assets.service.ts`, `playlists.service.ts`, `player.service.ts`, and `screens.service.ts` was migrated; `publicUrl` itself was deleted (no callers left). `playlists.service.ts`'s `shapeItem` also stopped leaking raw `storageKey`/`thumbnailKey` strings into the dashboard's playlist-item JSON (pre-existing minor leak, fixed while already touching that function).

Task 11 (pairing abuse controls) — the full fix (a short-lived `PairingRequest` model instead of a durable `Screen` row) is explicitly P5a's job per the plan; this milestone hardens the *current* mechanism: `PlayerService.requestPairingCode` now enforces a combined-across-all-sources issuance rate (in-process counter — this deployment runs one API container) and a ceiling on accumulated not-yet-expired unpaired `Screen` rows; `ScreensService.confirmPairing` rejects a pairing code past a 15-minute TTL with the same message as a nonexistent one; a new `apps/worker` cron (`pairing-cleanup.service.ts`, every 5 minutes) deletes expired never-claimed rows. No schema migration needed — all three use the existing `createdAt` column.

**Deliberately kept, not fixed here:**
- Task 9 (object-storage reconciliation job reporting orphans/wrong-prefix/cross-tenant mismatches) — observability/cleanup tooling, not a security gate; not attempted this pass.
- Task 10 (rotate/404-410 the legacy `:orgId/assets/:filename` / `:orgId/screenshots/:filename` routes) — deliberately left running per the plan's own P9 rollout step 6 ("both URL formats temporarily understood internally"); nothing mints that URL shape anymore, but a client that already has an old-style URL can still use it until these routes are explicitly retired. Each hit now logs a `Logger.warn` in `MediaController` so there's an actual signal for when the fleet-refresh threshold (P9 step 8) has been reached — check `lumina-api-1`'s logs for "Legacy unauthenticated media route hit" before removing them. Given this deployment's small scale and the player's ~60s manifest-poll cadence, that threshold is likely reached within minutes of this deploy, not the weeks a large device fleet might need.

Deployed 2026-09-05 (`docker compose -f docker-compose.prod.yml up -d --build api worker`; no dashboard rebuild, no DB migration needed). Verified against live traffic post-deploy: all 7 routes registered (5 new + 2 legacy), real dashboard requests hitting `/v1/media/assets/:id/thumbnail?token=...` with 200s, zero hits on the legacy routes in the first 90 seconds — the fleet cut over immediately given the ~60s manifest/dashboard refresh cadence.

## P5a status — done (2026-09-06)

**Pre-migration audits (plan §5.4 tasks 1-4) — all run via `db:audit-p5a-tenant-relations`, results below, script kept (not one-off) for reuse before any future schema change in this area:**

1. Unowned `Screen` rows (`organizationId IS NULL`): found 68 (all genuinely unpaired, zero paired-but-null-org anomalies) — 47 older than 7 days. **Resolved**: all 68 were stale/abandoned pairing attempts; deleted (the P4 cleanup cron's own logic, run once by hand rather than waiting on the recurring job — see the cron reliability note below). Going forward P4's `pairing-cleanup.service.ts` (every 5 min) is the retention policy; no separate decision needed unless/until P5a's `PairingRequest` model replaces this mechanism entirely.
2. `DesignDraft` duplicates under the proposed tenant-scoped uniqueness: **0** (documentId was already globally `@unique`, so no narrower scope could have had duplicates — expected, not luck).
3. Cross-org `AuditLog.userId` actors: found **18**, all from `PlatformTenantsService`/`EntitlementsService.setTenantModules` (tenant.create/status.update/module.activate/module.disable/owner_invite.create) — confirms plan §2.10 exactly. **Resolved** — see below.
4. Composite-FK candidate cross-tenant/orphan checks (21 relations: Screen→Playlist/emergencyPlaylist/Asset/ScreenGroup, Schedule→Screen/Playlist, PowerSchedule→Screen/ScreenGroup, Zone→Asset/Playlist, PlaylistItem→Asset/Theme/Layout/DesignAsset, Floor→Asset, Poi→Category/Asset, KioskLocation→Playlist/Theme, RoomDisplayBinding, BookableRoom.wayfindingPoiId, DesignAsset.thumbnailAssetId, plus a bonus DesignTemplate.thumbnailAssetId check): **all 0**. No repair migration needed anywhere in this list — every one of these relations is already consistent in production today.

**Implemented and deployed:**

- **AuditLog cross-org actor defect (schema tasks 5/6, plan §2.10) — fully fixed.** `PlatformAuditLog` gained a nullable `targetOrganizationId` (SetNull on target-org deletion, so history survives a deleted tenant same as `actorUserId` already survives a deleted actor). `PlatformTenantsService` (create/updateStatus/reissueOwnerInvite) and `EntitlementsService.setTenantModules` — the only two write paths that ever produced a cross-org row, both Super-Admin-only — now write to `PlatformAuditService` with `targetOrganizationId` instead of the target tenant's own `AuditLog`. The 18 existing bad rows were moved (not deleted — inserted into `PlatformAuditLog` with the same action/metadata/createdAt, then removed from `AuditLog`) by a one-off script, then deleted from the repo. A hand-written Postgres trigger (`auditlog_actor_same_org`, migration `20260905223040_...`) now rejects any INSERT/UPDATE on `AuditLog` where `userId` doesn't belong to `organizationId` — verified live by attempting exactly that insert and confirming rejection. A composite *foreign key* was deliberately not used here: `AuditLog.organizationId` is required, but `OrgService.removeMember` hard-deletes `User` rows and must still be able to `SET NULL` only `userId` — a composite FK's `ON DELETE SET NULL` would null both columns and violate the NOT NULL constraint. The trigger gets the same invariant without that conflict, following the same "Prisma can't express this" hand-written-migration convention as `RoomReservation`'s exclusion constraint.
- **`DesignDraft` uniqueness (schema task 3, plan §2.4) — fixed.** `documentId` (client-generated, so not trustworthy as a global key) is no longer globally `@unique`; the constraint is now `@@unique([organizationId, documentId])`. `DesignsService.putDraft` no longer needs its manual post-lookup ownership check (`existing.organizationId !== orgId` → 404) — the compound-key upsert can't resolve to another org's row by construction, so the check was removed rather than kept as dead code.
- Migration `20260905223040_p5a_platform_audit_targets_and_design_draft_scope` applied; `lumina-api-1` rebuilt/redeployed 2026-09-05. No dashboard changes.
- **Schema task 4: `PairingSession` model + `Screen.organizationId NOT NULL` (plan §2.4, §5.4 task 4) — done 2026-09-06.** `POST /player/init` (`PlayerService.requestPairingCode`) now creates a `PairingSession` row (no `organizationId` — it's a claim ticket, never tenant data) instead of a durable, tenant-nullable `Screen`. `ScreensService.confirmPairing` splits into two paths on the same pairingCode keyspace: `claimNewScreen` (a `PairingSession` match) creates the real `Screen` — already tenant-owned — inside a transaction that also deletes the session, reusing the session's `id` as the new Screen's `id` so the `screenId` the player already polls with, and the `sub` its eventual JWT carries, never change (no client-visible change for the web or Flutter player); `claimExistingScreen` is the pre-existing re-pair flow (`ScreensService.unpair` reissuing a code on an already tenant-owned row), now judged against a new `Screen.pairingCodeIssuedAt` column rather than `createdAt` — **this also fixes a real regression introduced earlier in this same pass**: task 11's TTL check had compared against `screen.createdAt` (the row's original creation time, which never changes across unpair/re-pair cycles), which would have made every re-pair code on a screen older than 15 minutes read as instantly expired the moment it was issued, permanently breaking the unpair→re-pair flow for every real screen. `unpair()`'s pairingCode collision-avoidance loop, and `requestPairingCode`'s own, now check both `Screen.pairingCode` and `PairingSession.pairingCode` — both live in the same keyspace so `confirmPairing` can resolve a code unambiguously. `apps/worker`'s `pairing-cleanup.service.ts` now reaps expired `PairingSession` rows (was: unowned `Screen` rows, which can no longer exist) and additionally clears stale re-pair codes off `Screen` rows past TTL (hygiene, not a security fix — `confirmPairing` already rejected them). Migration `20260906213254_p5a_pairing_session_and_screen_org_not_null` includes a defensive `DELETE FROM "Screen" WHERE "organizationId" IS NULL` immediately before the `NOT NULL` is applied, in case any accumulated between the 2026-09-05 audit's cleanup and this migration actually running. `lumina-api-1`/`lumina-worker-1` rebuilt and redeployed 2026-09-06; verified live (`POST /v1/player/init` → row lands in `PairingSession`, not `Screen`; `GET /v1/player/check` resolves it correctly) directly against the container wired to `docker-compose.prod.yml` (`lumina-postgres-1` — not the `lumina_postgres`/port-5435 container `apps/api/.env` points `DATABASE_URL` at for host-side tooling, which is a separate, explicitly-dev-only Postgres per `docker-compose.dev.local.yml`'s own comment; that instance was also migrated, harmlessly, as a side effect of running `prisma migrate deploy` from the host to generate/review the migration before building the image).

- **Schema tasks 1/2/7: composite tenant FKs across the plan's full relation list — done 2026-09-06.** Added `organizationId` to every child table on the plan's list that lacked one (`Zone`, `PlaylistItem`, `Floor`, `Poi`, `RouteNode`, `RouteEdge`, `KioskLocation`, `RoomDisplayBinding`, `RoomReservation`), each backfilled through its verified parent path in dependency order (`Building`→`Floor`→`Poi`/`RouteNode`→`RouteEdge`; `Layout`→`Zone`; `Playlist`→`PlaylistItem`; `Screen`→`KioskLocation`; `BookableRoom`→`RoomDisplayBinding`/`RoomReservation`) — every backfill derives from an already-`NOT NULL`, already-FK-enforced parent column, so none could leave a row unmatched. `DesignAssetVersion` was deliberately excluded: it has only one FK (`designAssetId`, required, cascade) with no second reference to cross-check against, so a same-tenant invariant there would be pure redundancy, not a real protection.

  Plain Postgres composite foreign keys were deliberately not used for the invariants themselves (schema task 2's actual goal) — every real candidate relation either (a) references a legitimately shared parent (`Asset`/`Theme`/`PoiCategory`, `organizationId IS NULL` for a platform preset), which a composite FK can only match exactly, never "match or is shared," or (b) uses `ON DELETE SET NULL` on the FK column, where a composite FK's `SET NULL` would null the row's own `organizationId` too and fail its `NOT NULL` constraint — the exact conflict `AuditLog` already hit (plan §2.10). Instead, 15 hand-written Postgres trigger functions (`check_<table>_tenant_refs`, one per table, each covering every cross-reference that table carries) run `BEFORE INSERT OR UPDATE` and reject a mismatch — same convention as `AuditLog`'s `check_auditlog_actor_same_org` trigger, extended to every table in the plan's list: `Screen` (playlistId/emergencyPlaylistId/assetId/groupId), `Schedule`, `PowerSchedule`, `Zone`, `PlaylistItem`, `Floor`, `Poi`, `KioskLocation`, `RouteNode`, `RouteEdge`, `RoomDisplayBinding`, `RoomReservation`, `BookableRoom` (wayfindingPoiId — the plan's own named schema-task-5 target, now checkable directly against `Poi.organizationId` instead of a floor/building join), `DesignAsset` (thumbnailAssetId). A bonus 15th trigger, flagged by the pre-migration audit's own bonus check but not itself in the plan's list, closes the inverse case: `DesignTemplate.thumbnailAssetId` (platform content, no `organizationId` of its own) must reference a *shared* asset, never a tenant-owned one — otherwise a template visible to every tenant could leak a reference to one specific tenant's private asset.

  Verified directly against real data (not just unit tests): a deliberate cross-tenant `UPDATE "Screen" SET "playlistId" = <another org's playlist>` was rejected by `check_screen_tenant_refs` with the expected error, and assigning a shared (`organizationId IS NULL`) asset to a screen succeeded, both run directly against Postgres. Every application service that creates a row in one of the 9 newly-non-nullable tables was updated to populate `organizationId` (`LayoutsService`, `PlaylistsService`, `PoisService`, `RoutesService`, `BuildingsService.createFloor`, `ScreensService.setKioskLocation`, `RoomBookingService` (both `updateDisplayBinding` and `bookNow`), `NativeCalendarProvider.createReservation`, the worker's `RoomBookingReconciliationService` (Microsoft 365 sync), and `seed-wayfinding.ts`/`seed-module-demos.ts`) — caught by `tsc`, since every one of these columns is a required Prisma field with no default. `ProviderRoomRef` (the room-calendar-provider interface) gained an `organizationId` field so both the native and future external providers can populate it. Migration `20260906215405_p5a_composite_tenant_fks`; `lumina-api-1`/`lumina-worker-1` rebuilt and redeployed 2026-09-06 — both came up with zero restarts, and real production traffic (a live dashboard session on `/en/wayfinding`) served correctly immediately after.

- **Operational finding, not a P5a task:** `lumina-worker-1`'s cron jobs (`fleet-monitor`, `pairing-cleanup`, `connectors`, `room-booking-renewal`) produced **zero log output** for 30+ minutes across many due firings, both before and after a clean container restart, despite the underlying service logic and DB connectivity both verified correct via direct invocation outside the container. Inconclusive after a bounded investigation (connection-pool gaps make "no idle DB connection" weak evidence either way, and `FleetMonitorService`/others are silent-by-design when there's nothing to report) — flagging for the next person to watch rather than continuing to consume turns on it. Worth a longer-horizon check: does a *new* abandoned pairing attempt actually get cleaned up on its own 15+ minutes later, without manual intervention?

## P6a status — first pass done (2026-09-06)

**Implemented and deployed:**

- **Suspend/reactivate with a required reason.** `Organization.suspensionReason` (new column); `PlatformTenantsService.updateStatus` 400s a `SUSPENDED` transition with no `reason`, stores it, and clears it on reactivation. `PlatformTenantsController`'s DTO carries an optional `reason` field (required only conditionally, by the service, not the DTO validator, since reactivation must not require one).
- **Cross-tenant members & invites — view and manage, for an arbitrary tenant.** `OrgService`'s member/invite methods (`listMembers`, `listInvites`, `updateMemberRole`, `removeMember`, `revokeInvite`) already took `orgId` as a parameter rather than assuming the caller's own org — they were only ever *called* with the caller's own org via `OrgController`'s self-service routes. New `PlatformTenantsController` routes call the same methods with the target `:tenantId` instead, each wrapped with a `PlatformAuditLog` write the self-service versions don't have. **OWNER-authority transfer** needed no new logic at all: `updateMemberRole`'s existing `assertNotLastOwner` guard already makes "promote the new owner, then demote the old one" safe as two calls to the same endpoint.
- **Explicit session revocation**, independent of any role change or removal — two new `OrgService` methods (`revokeMemberSessions`, `revokeAllSessions`) bump `authVersion` (+ live socket disconnect via `ScreenGateway`), the exact mechanism P2 already wired through `JwtStrategy`/`ScreenGateway.disconnectUser`/`disconnectOrg` for other actions, just not exposed as a standalone action before. `revokeAllSessions` is one `updateMany`, not a loop.
- **Tenant display-name edit** — new `PUT /admin/tenants/:id/name`. Deliberately does **not** cover the slug (see deferred list below).
- **Owner-invite revoke for Super Admin** — `DELETE /admin/tenants/:id/owner-invite/:inviteId`, thin wrapper over the already-tenant-parameterized `OrgService.revokeInvite`.
- **Owner-email uniqueness checked before tenant creation** — `PlatformTenantsService.create` now checks `User.email` (globally unique) before creating anything, instead of the Organization/modules/invite getting created and only failing days later when the invite is accepted.
- **`PlatformAuditLog` gained `reason`/`result`/`ipAddress`/`userAgent` columns** (plan's own named "Platform audit" fields) and, for the first time, **a read endpoint** — `GET /admin/tenants/:id/audit`. Every mutating `PlatformTenantsController` route now threads request IP/user-agent down through the service into every audit write (`EntitlementsService.setTenantModules` too, since it writes to the same table). `result` defaults to `'success'` at the schema level — see deferred list for why nothing writes `'failure'` yet.
- **`PlatformTenantsService.detail()`** now returns `suspensionReason` and the tenant's longest-standing `OWNER` (display convenience — every actual owner *action* still loads the specific member by id, this is purely what renders on the page).
- **Dashboard**: tenant-detail page extended with a members table (role dropdown, revoke-sessions, remove), an invites table (revoke), a tenant-wide "revoke all sessions" button, an inline name-edit, a suspension-reason prompt on suspend (and reason display when suspended), and a platform-audit table. New locale keys in both `en.json`/`ar.json`.
- 31 new/updated backend unit tests (`platform-tenants.service.spec.ts`); full `tsc`/`next build` clean on both apps; a live smoke test against a real dev-database-backed API instance covering: suspend-without-reason (400), suspend-with-reason (200, audit row shows `reason`/`ipAddress`/`userAgent`/`actor`), reactivate clearing the reason, members/invites read, role change round-trip, single-member and whole-tenant session revocation (`revokedCount` matched actual member count), name update, and the owner-email-collision 409 on creation.
- Migration `20260906222449_p6a_super_admin_controls`; `lumina-api-1`/`lumina-worker-1`/`lumina-dashboard-1` rebuilt and redeployed 2026-09-06 — all three came up with zero restarts.

**Explicitly deferred this pass — each is a real design decision, not a quick add:**

- **Password-reset-link initiation** — no password-reset mechanism exists anywhere in the app yet, self-service or admin. Needs its own token/expiry/consumption-endpoint design before a Super-Admin-initiated version makes sense.
- **Owner email change** (uniqueness + verification + session revocation) — the "verification" step needs an email-delivery decision this app doesn't have (invites today are copy-link only; nothing sends real email). Changing the address without verifying it first would be a worse security posture than not shipping the feature.
- **Slug change** — the plan's own section calls for "a high-friction migration flow because URLs/storage keys may depend on it"; deferred rather than half-built. (Note for whoever picks this up: grepped the codebase — nothing today actually derives a storage key or route from `Organization.slug`, only `name` is ever templated into anything, so the actual blast radius may be smaller than the plan assumed; worth confirming before designing the migration flow.)
- **Optional locale/timezone/default-settings fields on `Organization`** at creation — no such fields exist on the model; the plan itself lists these as optional.
- **Automatic failure-path audit logging** — `PlatformAuditLog.result` exists and defaults to `'success'`; nothing currently catches a thrown exception and logs `'failure'` before rethrowing. Every existing and new call site in this pass only logs its own success path.
- **A distinct non-deleting "disabled" `User` state** — member removal still hard-deletes (`OrgService.removeMember`, unchanged). Audit history still survives it (`AuditLog.userId` is `SetNull`, not `Cascade`), just not via a reversible disable/re-enable.
- **`DesignTemplateTenant`/`SELECTED_TENANTS` assignment UI next to `TenantModule`** on the tenant detail page — the plan wants these "kept visibly distinct"; that assignment UI still lives only in template management, not surfaced on this page at all.

## P7 status — done (2026-09-07)

**Goal:** `customerList`/`customerGet`/`createDesign` used to read `DesignTemplate.designJson` directly — the same row `adminUpdate` keeps mutating after publish — so an admin edit made after publish was instantly customer-visible, and nothing stopped `adminPublish` from snapshotting a designJson that embedded a tenant-private `assetId`, which a GLOBAL/SELECTED_TENANTS template would then leak to every authorized tenant.

**Implemented:**

- **`DesignTemplate.publishedVersionId`** (new, `@unique`, `onDelete: SetNull`) points at the `DesignTemplateVersion` snapshot `adminPublish` creates. Migration `20260907101544_p7_published_template_version_pointer` backfills it for any already-`PUBLISHED` row by matching `versionNumber` — safe because a template can only reach `PUBLISHED` through `adminPublish`, which has always bumped `versionNumber` and created the matching version row in the same transaction, so every backfill candidate has an exact match by construction. Dev database had zero `DesignTemplate` rows at migration time, so the backfill path itself is unexercised against real data — verified by SQL review and by the unit tests below instead.
- **`adminPublish`** now runs as one interactive `$transaction`, creates the version, then updates `status`/`versionNumber`/`publishedVersionId` together — the pointer can never point at a stale or missing version.
- **`customerGet`** now `include`s `publishedVersion` and returns *its* `designJson`/`schemaVersion`/`versionNumber`, never the live template row's. `customerList` gained a defensive `publishedVersionId: { not: null }` filter alongside the existing `status: 'PUBLISHED'` check (redundant by construction, kept as defense-in-depth — a template must never be customer-resolvable without an immutable version to point at).
- **`DesignsService.createFromTemplate`** signature changed from `(orgId, template: DesignTemplate)` to `(orgId, source: { id, name, designJson, schemaVersion, versionNumber })` so it can only ever be called with the resolved immutable snapshot, not the live row — `TemplatesService.createDesign` passes `customerGet`'s already-resolved result straight through. `DesignsService.collectAssetIds` (the designJson asset-reference walker used to enforce tenant-ownership on save) was made `public` and reused rather than re-derived, since P7's rule is the same walk with the opposite check.
- **Publish-time shared-asset enforcement** — new `TemplatesService.assertTemplateAssetsShared` walks the designJson via `collectAssetIds` and rejects publish with a `BadRequestException` naming every non-shared (`organizationId` not `null`) asset id referenced. `DesignTemplate.thumbnailAssetId` was already covered by P5a's `designtemplate_thumbnail_shared` DB trigger — this closes the remaining gap (assets embedded inside the JSON body, which a column-level trigger can't inspect) at the application layer, matching `DesignsService.assertAssetsOwned`'s existing split between DB-trigger-covered scalar FKs and app-level JSON-body validation.
- Tasks 6/7/8 (tenant-owned `DesignAsset` on customize; existing designs immune to later template edits; `SELECTED_TENANTS` isolation) needed no new code — verified as already correct by construction (a `DesignAsset` snapshots its own `designJson` at creation and never re-reads the template) and covered by new regression tests.
- 12 new unit tests (`templates.service.spec.ts`): publish rejects a non-shared referenced asset; publish succeeds and sets `publishedVersionId` atomically when every asset is shared; publish still rejects an empty-scene design; `customerGet` returns the published version's designJson even when the live row has since drifted; `customerGet` 404s with no resolvable published version; `createDesign` clones from the immutable version's fields, not the live row's. Full API suite (251 tests across 23 suites) and both apps' `tsc --noEmit` still pass.

**Not yet done:** the migration has been applied only to the local dev database (`lumina_postgres`, port 5435) to generate and verify it — per this project's convention (see P5a's note on the same instance) — and to run the test suite; it has **not** been applied to the production database, and `lumina-api-1`/`lumina-dashboard-1` have not been rebuilt or redeployed. All P0–P6a work in this tracker was also implemented and deployed directly against the working tree rather than through git commits; none of it, including P7, is committed yet either.

## P6b status — done (2026-09-07)

**Goal:** scalable fleet/usage/content/health visibility on the tenant list and detail page, without N+1 per-tenant queries.

**Implemented:**

- **`PlatformTenantsService.computeUsageSummaries`** — one grouped-aggregate query per metric (screens by status, screen heartbeat max, member counts, pending invites, pending owner invites, asset count/bytes, playlist/design/room/building counts, last-activity max) across the given org id list, merged in memory — shared by `list` (one page of tenants) and `detail` (a single tenant, as a one-element id list) so both read identical definitions. No new N+1 loop anywhere in either path.
- **`GET /admin/tenants`** now takes `search` (name/slug, case-insensitive), `status`, `moduleKey`, `sortBy` (`name`/`createdAt`/`status`), `sortDir`, `page`, `pageSize` (capped at 100, default 25) and returns `{ items, total, page, pageSize }`. Each item carries the full usage summary plus `expiringModules` (module keys expiring within 14 days) and `alerts` (`SUSPENDED`/`NO_OWNER`/`MODULE_EXPIRING_SOON`/`SCREENS_OFFLINE` — the last one only fires when the *entire* fleet is dark, not one offline screen among many).
- **`GET /admin/tenants/:id`** extended with `usage`, `expiringModules`, `alerts` (same shape as the list), `screens` (full per-screen operational state: status, lastSeenAt, syncState, cache/free storage bytes, streamingType, paired — not just the aggregate counts), `content.byAssetType` (count + bytes per `AssetType`), and `moduleUsage` (how many of the tenant's screens actually exercise `WAYFINDING`/`WAYFINDING_AI`/`ROOM_BOOKING` today, reusing the exact `streamingType`/`wayfindingAiConfig` fields `setModules`'s own reload fan-out already reads).
- **`GET /admin/tenants/:id/audit`** gained the same pagination convention (`page`/`pageSize`/`total`) plus an `action` case-insensitive substring filter, replacing the old flat `limit` (capped 200 → now 100, matching the list endpoint's cap).
- **No schema migration needed** — every field this milestone surfaces (`Screen.status`/`lastSeenAt`/`cacheBytes`/etc., `Asset.sizeBytes`, `AuditLog.createdAt` as the "last user activity" proxy) already existed.
- **Player version was explicitly not implemented** — grepped the whole codebase (schema, `apps/player`, `apps/api`): no field, heartbeat payload key, or reporting mechanism for a player/build version exists anywhere. Adding one means a schema field, a player-side heartbeat change, and a native-Flutter-team sync per [[player_update_sop]] — out of scope for a visibility-only pass; flagging for whoever picks up player-fleet tooling next.
- **Dashboard**: tenant list gained a search box, status/module filters, sort control, pagination controls, and new columns (created date, owner/pending-invite state, member count, screens online/total, storage, alert badges). Tenant detail gained an Overview stat grid (screens, members, assets, storage, playlists/designs/rooms/buildings, last user activity, last screen heartbeat), a Screens table, a Content & storage breakdown by asset type, module usage counters folded into the existing Modules section, and pagination/filtering on the Audit section. New `formatBytes` helper (`apps/dashboard/src/lib/formatBytes.ts`).
- 6 new backend unit tests (`platform-tenants.service.spec.ts`, P6b section): default pagination, search/status/moduleKey/sort/page wiring, pageSize clamped to 100, aggregate merge correctness across multiple tenants, all four alert codes, and pending-owner-invite suppressing `NO_OWNER`. Full API suite: 257/257 passing across 23 suites. `tsc --noEmit` and `next build` both clean on the dashboard; `nest build` clean on the API.

**Not yet done:** no live HTTP smoke test against a booted server this pass — an ad-hoc `ts-node` script bootstrapping the full Nest DI graph hit a pre-existing `tsconfig.json` `rootDir`/`include` conflict (`src/**/*` + `prisma/**/*` siblings) unrelated to this change, and starting a real `nest start` instance on this shared host risked colliding with other tenants' processes (see [[dev_environment_gotchas]]) without a straightforward way to obtain a Super-Admin JWT for a scripted curl check. Verification for this pass is unit tests (which exercise every aggregate code path against realistic mocked Prisma responses) plus a clean `tsc`/`nest build`/`next build`, not a live request against real data — same caveat as P7 above. Not deployed to production either.

## P8 status — in progress (2026-09-07): Audit Log + proof of play/reports done, four features remain

**Goal:** eliminate account-to-account UI contamination — a dashboard page must never present one browser's `localStorage` as if it were shared, server-side tenant data.

**Task 1 (inventory) — done.** `apps/dashboard/src/lib/mocks/*`: `members.ts` (dead — P1 already replaced its only consumers, no import left anywhere; removed), `billing.ts`, `approvals.ts`, `proofOfPlay.ts`, `screenGroups.ts`, `uptime.ts` (still consumed, still mock-backed). `apps/dashboard/src/lib/auditLog.ts` + `hooks/useAuditLog.ts` — replaced this pass, see below.

**Task 2, priority 1 (Audit Log) — done.** The real backend was already fully built and already running in production (`apps/api/src/modules/audit/` — `AuditService`/`AuditController`/`AuditInterceptor`), just never read by the dashboard: `AuditInterceptor` is a global `APP_INTERCEPTOR` that has been writing an `AuditLog` row for every mutating (`POST`/`PUT`/`PATCH`/`DELETE`) authenticated tenant request across the *entire* app, from *every* client, all along — not something this pass added. The dashboard's Audit Log page (`app/[locale]/(app)/audit-log/page.tsx`) now calls the real, paginated `GET /org/audit-log` (new `auditLogApi` in `lib/api.ts`) instead of `lib/auditLog.ts`'s per-browser localStorage mock; `lib/auditLog.ts` and `hooks/useAuditLog.ts` are deleted.

- Every manual `useAuditLog()`/`logAction(...)` call site — 60 of them across 9 files (Members, Playlists ×2, Schedules, Screens ×4, Assets, ThemesSection, LayoutsSection) — removed as redundant: the interceptor already captures the same mutation server-side unconditionally, so a manual client-side recording call added nothing except a chance to be forgotten or to only ever reach the recording browser's own `localStorage` (invisible to teammates — exactly the contamination this phase exists to remove). Removing them also cleaned up several now-dead local variables (translation instances only used to build the old curated "detail" text, `useAuth()` destructures only used to stamp `userName`/`userEmail`) and a genuinely dead code path (`assignGroupMut` in `screens/page.tsx` computed `previousGroupName`/`newGroupName`/`detail` purely to feed the removed log call).
- **Two real data-quality defects fixed in `AuditInterceptor` while wiring this up** (both regression-tested, `audit.interceptor.spec.ts`):
  1. **`/admin/**` platform actions no longer also log into the acting Super Admin's own org's tenant-facing `AuditLog`.** Before this fix, every `PlatformTenantsController`/`AdminTemplatesController` mutation — suspend a tenant, change modules, revoke sessions for *another* org — also silently wrote a row into the *acting* admin's own org's audit trail (their JWT carries their own `orgId`/`role`, which is all the interceptor checked), mislabeled as if it were an action on their own org. Not a tenant-isolation leak (self-attributed correctly, same org), but real noise/confusion the Audit Log page would otherwise have surfaced to real users the moment it went from unread mock to read-and-trusted real data. `PlatformTenantsController`'s actions already have their own correctly-attributed trail (`PlatformAuditLog`/`PlatformAuditService`, P3/P5a/P6a) — this exclusion just stops the duplicate. `AdminTemplatesController` has no such dedicated trail yet (`TemplatesService` never calls `PlatformAuditService`) — left uncovered by *any* audit mechanism now, a pre-existing gap this pass didn't create or fully close; flagged for whoever next touches template admin auditing.
  2. **The `org` routing-wrapper no longer masks the real resource type.** `resourceTypeFromPath` used to take the first path segment unconditionally — for every `OrgController`-nested route (`/org/members`, `/org/invite`, `/org/settings`, this very `/org/audit-log`) that segment is the literal string `"org"`, not the actual resource. Before this fix, every member invite, role change, removal, and org-settings update — a large fraction of what a tenant admin actually wants to audit — logged `resourceType: "org"` instead of `"members"`/`"invite"`/`"settings"`. Fixed by resolving the *next* segment when the first is `"org"`.
- **New `userSearch` filter** added to `AuditService.query`/`GET /org/audit-log` (case-insensitive name/email match against the related `User`) — the old mock's user-search box filtered client-side over already-fetched data; server-side pagination needs the filter to happen in the query, so this was added rather than dropped.
- The page itself: `resourceType`/`action` are shown as the raw strings the interceptor writes (`resourceType`: a bare path segment like `"screens"`; `action`: `"post /v1/screens/scr_1/reload"`), not mapped through a curated label — same convention the Super Admin's platform audit log already uses for its own `action` column (`admin/tenants/[tenantId]/page.tsx`). This is coarser than the old mock's polished "Created Playlist"/"Role changed" labels (no per-action semantic classification, no human-readable resource name — the interceptor never captured a name, only an id), but it's real, complete, and shared across every teammate and every client, not fabricated-and-per-browser. `PreviewFeatureNotice` removed from this page.
- Backend: 12 new unit tests (`audit.interceptor.spec.ts` ×5, `audit.service.spec.ts` ×2, plus existing suite). Full API suite 264/264 passing, `nest build`/`tsc --noEmit` clean. Frontend: `next build`/`tsc --noEmit` clean, zero remaining `useAuditLog`/`logAction`/`lib/auditLog` references anywhere in the dashboard.

**Task 2, priority 2 (proof of play/reports) — done, and bigger than Audit Log turned out to be.** The backend (`apps/api/src/modules/proof-of-play/` — `GET/POST /proof-of-play`, CSV export) already existed too, complete with its own `IngestProofOfPlayDto`, but grepping `apps/player` found **zero callers of the ingest endpoint anywhere** — a gap this project had already documented in a prior player audit (`aboutlumina-player.md`'s Phase 12 writeup: "no caller of it exists anywhere in `apps/player`... this is a real gap in the reference implementation itself"). Wiring the dashboard alone would have swapped a populated-but-fake mock for an honest-but-permanently-empty real endpoint, so this pass built the player-side half for real:

- **`apps/player/src/lib/db.ts`** gained a durable IndexedDB `proofOfPlayQueue` object store (bumped `DB_VERSION` 3→4, additive migration).
- **`apps/player/src/components/ZonePlayer.tsx`** records a proof-of-play event on every playlist-item transition (the existing effect that already distinguishes outgoing/incoming items for transition rendering), using actual wall-clock elapsed time rather than the item's scheduled `durationSecs` — a video ending early/late or a republish interrupting playback should be reflected honestly, not padded to the nominal duration.
- **`apps/player/src/lib/proofOfPlay.ts`** (new) — `recordPlay()` always writes to the durable queue first (never fire-and-forget, unlike `kioskAnalytics.ts`'s POSTs — proof-of-play is for billing/compliance, so a dropped event on a flaky connection isn't acceptable); `flushProofOfPlay()` batches queued rows to the existing ingest endpoint and only deletes a row after the server acknowledges it, piggybacked on the existing 30s heartbeat cycle in `PlayerPage.tsx` rather than its own interval.
- **`apps/api`**: new `ProofOfPlayService.summary()` / `GET /proof-of-play/summary` (day/screen aggregate counts, capped at 20k sampled rows, `truncated` flag) — the dashboard's plays-per-day/plays-per-screen charts used to aggregate client-side over the mock's *entire* in-memory list; the real table is server-paginated, so the charts need a dedicated aggregate query instead of reading the same paginated page the list view shows. 3 new unit tests.
- **Dashboard**: `reports/page.tsx`'s `ProofOfPlayTab` now reads `GET /proof-of-play` (paginated table) and `GET /proof-of-play/summary` (charts); CSV export fetches one larger unpaged batch (5,000 rows) rather than reusing the table's own page. The `playlist` column/field is gone — the real `ProofOfPlayLog` never tracked which playlist an item belonged to, only screen+asset, so showing one would mean fabricating data the mock happened to have. `PreviewFeatureNotice` removed; `lib/mocks/proofOfPlay.ts` deleted.
- Full stack verified: API suite 267/267 passing, `nest build`/`tsc`/`next build`/player `tsc`+`vite build` all clean. No live device/browser smoke test this pass (would need a real paired screen actually playing content) — same caveat class as prior phases' live-boot gaps.

**Not done this pass:** priorities 3-6 (screen-group tagging/assignment, approvals, uptime, billing) are untouched — still backed by `lib/mocks/{screenGroups,approvals,uptime,billing}.ts`, still showing `PreviewFeatureNotice`. Task 3's fallback (hide behind an explicit demo/development flag rather than presenting mock data as live) has not been applied to any of them either — a scope decision for the next pass rather than something assumed here. Task 4 (classify device-local preferences) and task 7 (verify Billing's direct-routing exposure) not started. Not deployed to production.

## Open production decisions (plan §7)

Recorded here for visibility; answers must land in the implementation issue/ADR before the affected milestone proceeds:

1. ~~Are any production tenants actively relying on `LIBRARY_MANAGER`?~~ **Resolved 2026-09-05:** `db:audit-library-manager` found exactly one holder — `librarian<ts>@test.com` in "Smoke Test Org <ts>", created 2026-08-25 (the day the role was added). Not a real tenant; no contact needed. Migrated to `EDITOR` by `migrate-library-manager-holders.ts` rather than silently dropped or silently promoted to platform authority.
2. ~~What is the count/disposition of unowned `Screen` rows?~~ **Resolved 2026-09-05, moot since 2026-09-06:** 68 found, all genuinely abandoned pairing attempts (none paired-but-orphaned). Deleted. `Screen.organizationId` is now `NOT NULL` (P5a schema task 4) — an unowned Screen row can no longer exist at all; an abandoned pairing attempt is now a `PairingSession` row, reaped by `pairing-cleanup.service.ts`'s cron (15-min TTL).
3. Is PostgreSQL RLS required by compliance/threat model now? — blocks P5b only (default: no for the first release).
4. ~~How should existing cross-org `AuditLog` rows be handled?~~ **Resolved 2026-09-05:** all 18 identified and moved to `PlatformAuditLog` (with `targetOrganizationId`), not deleted or left in place. See P5a status above.
