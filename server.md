# تقرير فحص السيرفر

**تاريخ الفحص:** 22 أغسطس 2026  
**السيرفر:** Netcup RS 2000 G12  
**نوع الخدمة:** Root Server افتراضي يعمل عبر KVM، وليس خادمًا ماديًا مستقلًا (Bare Metal)

## 1. الهدف من الفحص

تم إجراء فحص عام للسيرفر لمعرفة سبب بطئه أو شبه تعليقه عند بناء مشروع Lumina داخل المسار:

```text
/opt/sites/lumina
```

شمل الفحص موارد النظام، العمليات، حاويات Docker، ضغط الذاكرة والقرص، سجلات النظام، وطريقة بناء المشروع.

> الفحص كان تشخيصيًا وللقراءة فقط. لم يتم إيقاف أو إعادة تشغيل أو حذف أو تعديل أي مشروع أو حاوية أو خدمة.

## 2. مواصفات السيرفر

- المعالج: AMD EPYC 9645
- الأنوية المتاحة: 8 أنوية مخصصة
- الذاكرة: 16 GB DDR5 ECC، ويظهر للنظام نحو 15.6 GiB
- التخزين: 512 GB NVMe
- Swap: ملف بحجم 4 GB
- نظام التشغيل: Linux داخل بيئة KVM

## 3. ملخص النتيجة

سبب التعليق المرجح هو **ضغط الذاكرة والانتقال الكثيف إلى Swap أثناء البناء**، وليس وجود مشروع يستهلك المعالج باستمرار، ولا امتلاء القرص.

السيرفر يشغل عددًا كبيرًا من خدمات الإنتاج بالتزامن مع بيئة تطوير كاملة تشمل VS Code Remote وأدوات TypeScript وClaude وCodex، إضافة إلى عمليات تطوير Lumina. عند بدء بناء Node.js وNext.js وعدة صور Docker، تظهر ذروة مؤقتة في استهلاك RAM تتجاوز الهامش المتاح.

عندما تمتلئ الذاكرة، يبدأ النظام بنقل صفحات الذاكرة إلى القرص عبر Swap. يؤدي ذلك إلى بطء شديد في SSH وVS Code والخدمات، وقد يبدو للمستخدم أن السيرفر توقف رغم أن المعالج لم يصل بالضرورة إلى استخدام كامل.

## 4. حالة الموارد وقت الفحص

### المعالج

- عدد الأنوية: 8
- متوسط الحمل كان قرابة: `1.64 / 1.65 / 1.33`
- كان المعالج خاملًا في أغلب عينات الفحص بنسبة تقارب 80% إلى 96%.
- لم يظهر مشروع يستخدم كامل المعالج بصورة مستمرة.
- لا توجد حاليًا علامة تدل على أن عدد الأنوية هو عنق الزجاجة الأساسي.

### الذاكرة وSwap

- إجمالي RAM: نحو 15.6 GiB
- المستخدم وقت القياس: نحو 10 GiB
- المتاح، شاملاً الذاكرة القابلة للاستعادة: نحو 5.5 GiB
- Swap المستخدم: نحو 3.5 GiB من أصل 4 GiB
- Swap المتبقي: نحو 500 MiB فقط

استخدام 3.5 GiB من Swap بعد نحو ساعتين من الإقلاع مؤشر واضح على حدوث ضغط ذاكرة ملحوظ. وجود بيانات داخل Swap لا يعني أن النظام كان تحت ضغط شديد في لحظة القياس نفسها، لكنه يدل على وصوله إلى ضغط مرتفع منذ الإقلاع.

إعداد `vm.swappiness` مضبوط على `10`، وهو إعداد محافظ ومناسب عمومًا. لذلك لا يبدو أن المشكلة ناتجة عن مبالغة النظام في استخدام Swap، بل عن حاجة فعلية إلى مساحة RAM إضافية أثناء الذروة.

### التخزين وعمليات الإدخال والإخراج

- المساحة الكلية: نحو 503 GB قابلة للاستخدام
- المستخدم: نحو 88 GB
- المتاح: نحو 395 GB
- نسبة الاستخدام: 19%

لا توجد مشكلة مساحة تخزين. كما لم تظهر أخطاء I/O أو أخطاء نظام ملفات في السجلات التي تم فحصها.

### ضغط النظام

مؤشرات PSI الخاصة بالمعالج والذاكرة وI/O كانت منخفضة وقت القياس. هذا متوافق مع كون التعليق يحدث كذروة أثناء البناء، وليس حملًا مستمرًا طوال الوقت.

## 5. حاويات Docker

- عدد الحاويات الموجودة: 57
- عدد الحاويات الفعالة وقت الفحص: 53
- صور Docker: 54 صورة بحجم إجمالي يقارب 24.16 GB
- Build cache: نحو 19.01 GB
- Volumes: نحو 2.66 GB

جميع الحاويات التي تم فحصها تعمل دون حدود صريحة للموارد:

- لا يوجد حد RAM.
- لا يوجد حد CPU.
- لا يوجد حد لعدد PIDs.

هذا يعني أن أي حاوية تستطيع استهلاك معظم موارد المضيف عند ارتفاع الحمل. لم تكن أي حاوية مسجلة على أنها قُتلت بسبب OOM وقت الفحص، ولم تظهر إعادة تشغيل غير عادية للحاويات بعد الإقلاع الحالي.

وجود 19 GB من Build cache لا يسبب ضغط RAM مباشرة، ولا توجد حاجة عاجلة إلى حذفه لأن مساحة القرص كافية. كما قد يساعد هذا الـ cache في تسريع البناء اللاحق.

## 6. أبرز مستهلكي الموارد

### بيئة تطوير Lumina

تم العثور على عمليات تطوير تعمل مباشرة على المضيف، بالتوازي مع حاويات الإنتاج:

- `next-server` داخل `/opt/sites/lumina/apps/dashboard`: نحو 1.58 GiB RAM.
- `nest start --watch` داخل `/opt/sites/lumina/apps/api`: نحو 456 MiB وقت القياس.
- نسخة API مبنية من `dist`: نحو 258 MiB وقت القياس.
- توجد كذلك حاويات الإنتاج الخاصة بـ Lumina.

هذا يعني وجود بيئة تطوير ونسخ إنتاج من المشروع تعمل في الوقت نفسه، وهو أمر مناسب للتطوير المباشر لكنه يقلل الهامش المتاح للبناء.

### VS Code Remote وأدوات التطوير

- عمليات مرتبطة بـ VS Code Remote: 43 عملية تقريبًا.
- مجموع RSS التقريبي لهذه العمليات: 5.77 GiB.
- خمس عمليات Claude: نحو 1.62 GiB مجتمعة.
- توجد عدة Extension Hosts وTypeScript servers وfile watchers.

قد توجد بعض الذاكرة المشتركة أو تداخل عند جمع RSS، لذلك لا تُعامل الأرقام المجمعة كقياس محاسبي دقيق، لكنها تبين بوضوح أن أدوات التطوير تستهلك عدة GiB.

### `comm-worker`

كانت حاوية `comm-worker` أبرز حاويات الإنتاج من ناحية الموارد:

- RAM: نحو 1.19 GiB.
- CPU وقت إحدى العينات: نحو 7% إلى 8%.
- PIDs: نحو 483.

تقوم هذه الحاوية بتشغيل عدة جلسات Chromium/Puppeteer، مع عمليات renderer وutility وGPU افتراضية متعددة. لم تكن تستنزف المعالج بالكامل، لكنها تضيف ضغطًا معتبرًا على الذاكرة وعدد العمليات.

يوصى بمراجعة دورة حياة المتصفحات داخل المشروع المسؤول عنها:

- التأكد من إغلاق الصفحات والمتصفحات بعد انتهاء المهمة.
- التحقق من عدم تراكم جلسات قديمة.
- استخدام browser pool بعدد أقصى واضح إن كان ذلك مناسبًا للتطبيق.
- قياس عدد المهام المتزامنة المطلوب فعليًا.

لا يوصي هذا التقرير بإيقاف الحاوية؛ المطلوب أولًا مراجعة سلوكها برمجيًا.

### Chromium

تم رصد نحو 26 عملية Chromium بمجموع RSS تقريبي 1.56 GiB. أغلبها تابع لـ `comm-worker` ويشغل عدة جلسات مستقلة.

## 7. سبب ارتفاع استهلاك بناء Lumina

يتكون مشروع Lumina من workspace يستخدم pnpm وTurbo، ويحتوي على:

- API مبني باستخدام NestJS وPrisma.
- Worker مبني باستخدام NestJS، مع متطلبات تشغيل مثل LibreOffice وPoppler وFFmpeg.
- Dashboard مبني باستخدام Next.js.
- Player مبني باستخدام TypeScript وVite.

ملف `docker-compose.prod.yml` يبني أربع صور تطبيق:

1. `api`
2. `worker`
3. `dashboard`
4. `player`

كل Dockerfile تقريبًا ينسخ ملفات workspace وينفذ `pnpm install --frozen-lockfile` داخل مرحلة البناء. بعد ذلك تنفذ الصور عمليات مثل:

- Prisma generate
- TypeScript compilation
- Nest build
- Next build
- Vite build

إذا تم بناء الصور بالتوازي، قد تعمل عدة عمليات `pnpm install` وTypeScript وNext.js في الوقت نفسه. هذه الذروة هي التفسير الأقوى للتعليق، خصوصًا مع بقاء خدمات الإنتاج وأدوات التطوير قيد التشغيل.

## 8. ملاحظات إضافية على Lumina

ظهرت مجموعتان من خدمات البنية الأساسية تحملان أسماء متقاربة:

```text
lumina_postgres
lumina_redis
lumina_minio
```

ومجموعة أخرى:

```text
lumina-postgres-1
lumina-redis-1
lumina-minio-1
```

قد تكون إحداهما للتطوير والأخرى للإنتاج، وهذا أمر ممكن ومقصود. استهلاكها الحالي منخفض، لكن يوصى بالتأكد من أن تشغيل المجموعتين مطلوب، دون إيقاف أي منهما قبل معرفة استخداماتها والـ volumes المرتبطة بها.

## 9. سجلات الأعطال وإعادة التشغيل

- لم يتم العثور على رسائل `Out of memory` أو `Killed process` في سجل kernel المتاح.
- لم تظهر أخطاء تخزين أو I/O مهمة.
- آخر إيقاف للسيرفر كان إيقافًا منظمًا أكمله systemd، وليس crash مفاجئًا ظاهرًا.
- لم تسجل الحاويات الحالية حالة `OOMKilled`.

عدم وجود OOM لا ينفي ضغط الذاكرة؛ يمكن أن يصبح النظام بطيئًا جدًا بسبب Swap قبل أن يقرر kernel قتل عملية.

## 10. التوصيات

### أولوية أولى: بناء Docker بشكل متسلسل

استخدام حد توازٍ مقداره واحد أثناء البناء:

```bash
COMPOSE_PARALLEL_LIMIT=1 docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d
```

هذا قد يزيد مدة البناء، لكنه يخفض ذروة RAM ويقلل تأثير البناء على خدمات الإنتاج.

### تقليل توازي Turbo

عند البناء مباشرة خارج Docker:

```bash
pnpm turbo run build --concurrency=2
```

يمكن البدء بقيمة `2`، ثم قياس الذاكرة والمدة قبل رفعها.

### مراجعة `comm-worker`

ينبغي تحليل إدارة Chromium/Puppeteer في هذا المشروع، وخصوصًا عدد الجلسات الدائمة وطريقة تنظيفها. هدف المراجعة ليس إيقاف المشروع، بل تقليل الاستهلاك غير الضروري ومنع تراكم العمليات.

### تنظيم جلسات التطوير

قبل تنفيذ بناء كبير يفضل، متى كان ذلك مناسبًا للعمل الجاري:

- التأكد من عدم وجود نوافذ VS Code Remote قديمة غير مستخدمة.
- التأكد من عدم وجود جلسات Claude أو Codex قديمة انتهى العمل عليها.
- تجنب تشغيل أكثر من TypeScript watcher للمشروع نفسه دون حاجة.
- معرفة ما إذا كان تشغيل `next dev` ونسخة production من التطبيق في الوقت نفسه مطلوبًا أثناء البناء.

لا ينبغي إنهاء أي عملية تلقائيًا؛ تتم مراجعتها يدويًا بحسب الجلسات المستخدمة فعليًا.

### حدود موارد Docker

يوصى لاحقًا بإضافة حدود مدروسة للخدمات، خصوصًا workers والخدمات غير الحرجة. يجب تحديد الحدود بعد مراقبة الاستهلاك في أوقات الحمل الحقيقي؛ وضع حد منخفض عشوائيًا قد يسبب OOM داخل الحاوية أو فشل مهام الإنتاج.

يمكن أيضًا إضافة حد PIDs مناسب لـ `comm-worker` بعد فهم العدد الطبيعي لعمليات Chromium.

### تحسين Dockerfiles

يمكن تقليل تكلفة البناء مستقبلًا عبر:

- استخدام `turbo prune` لإنتاج workspace مصغر لكل تطبيق.
- تقليل تكرار تثبيت dependencies في أربع صور مستقلة.
- تحسين الاستفادة من BuildKit cache وpnpm cache mounts.
- مشاركة مرحلة بناء أساسية مدروسة بين التطبيقات إن كان ذلك مناسبًا.
- الاستمرار باستخدام Next.js standalone output في Dashboard، لأنه يقلل حجم طبقة التشغيل.

## 11. هل يجب ترقية الخطة؟

إذا كان الهدف هو استمرار تشغيل جميع مشاريع الإنتاج، مع العمل عبر VS Code Remote وتشغيل أدوات AI وبناء Node.js وDocker مباشرة على السيرفر، فإن **الترقية إلى 32 GB RAM موصى بها**.

التقييم المقترح:

| الذاكرة | التقييم للحالة الحالية |
|---|---|
| 16 GB | قابلة للعمل مع بناء متسلسل وإدارة دقيقة لجلسات التطوير، لكن هامش الذروة ضيق |
| 32 GB | الخيار المتوازن والموصى به لطريقة العمل الحالية |
| 64 GB | مناسب إذا زاد عدد المشاريع أو جلسات Chromium أو أصبحت عمليات البناء متوازية ومتكررة |

لا توجد أدلة حالية على ضرورة ترقية المعالج تحديدًا. الأولوية عند اختيار خطة أعلى هي **زيادة RAM**، ثم يمكن تقييم عدد الأنوية بناءً على زمن البناء بعد حل ضغط الذاكرة.

## 12. القرار المقترح

1. تطبيق البناء المتسلسل أولًا ومراقبة سلوك السيرفر أثناء بناء كامل.
2. مراجعة استهلاك `comm-worker` وجلسات Chromium دون إيقاف الخدمة.
3. مراقبة RAM وSwap أثناء البناء الفعلي.
4. إن كان التطوير والبناء المباشر على سيرفر الإنتاج أسلوب العمل الدائم، الانتقال إلى خطة 32 GB حتى لو نجح البناء المتسلسل، لأن 16 GB لا تترك هامش أمان مريحًا للخدمات الحالية.

الاستنتاج النهائي هو أن مواصفات المعالج والتخزين جيدة، لكن حجم الذاكرة الحالي لا يتناسب بشكل مريح مع الجمع بين إنتاج متعدد المشاريع وبيئة تطوير وبناء Docker على المضيف نفسه.

---

## 13. تحديث بعد ترقية الخطة

**تاريخ القياس:** 22 أغسطس 2026  
**وقت القياس:** بعد نحو 25 دقيقة من إقلاع السيرفر بالخطة الجديدة

تمت ترقية الخطة بنجاح، وأصبحت الموارد التي يراها نظام التشغيل:

- المعالج: 12 نواة مخصصة بدلًا من 8.
- الذاكرة: نحو 31.34 GiB قابلة للاستخدام بدلًا من 15.61 GiB.
- Swap: بقي بحجم 4 GiB.
- التخزين: بقي نحو 503 GB، بنسبة استخدام 19%.

تمت إعادة الفحص دون إيقاف أو تعديل أي خدمة، وكانت جميع حاويات الإنتاج السابقة تقريبًا قيد التشغيل.

### مقارنة مباشرة قبل الترقية وبعدها

| المؤشر | قبل الترقية | بعد الترقية |
|---|---:|---:|
| الأنوية | 8 | 12 |
| RAM الكلية | 15.6 GiB | 31.34 GiB |
| RAM المتاحة وقت القياس | نحو 5.5 GiB | نحو 19 GiB |
| RAM المستخدمة | نحو 10 GiB | نحو 12 GiB |
| Swap المستخدم | نحو 3.5 من 4 GiB | 0 من 4 GiB |
| Load average للدقيقة الأخيرة | نحو 1.64 | نحو 0.66 |
| الحاويات الفعالة | 53 | 53 |
| مساحة القرص المستخدمة | 19% | 19% |

زيادة RAM المستخدمة من 10 إلى 12 GiB لا تمثل مشكلة؛ بعد إعادة الإقلاع استعادت الخدمات caches وذاكرة تشغيل جديدة، كما اختلفت أحمال التطبيقات قليلًا. المؤشر الأهم هو وجود نحو 19 GiB متاحة وعدم استخدام Swap إطلاقًا.

### ضغط النظام بعد الترقية

- ضغط الذاكرة PSI كان صفرًا تقريبًا.
- ضغط المعالج منخفض جدًا.
- ضغط I/O منخفض جدًا.
- أظهرت عينات `vmstat` أن المعالج خامل بنسبة تقارب 90% إلى 95%.
- لم تحدث عمليات Swap-in أو Swap-out أثناء القياس.
- لم تظهر رسائل OOM أو `Killed process` أو أخطاء I/O في الإقلاع الحالي.
- تمت إعادة التشغيل المصاحبة للترقية بصورة منظمة، وفق سجل الإقلاع والإيقاف.

هذه النتائج تؤكد أن الخطة الجديدة وفرت هامش موارد جيدًا لتشغيل الخدمات الحالية وبيئة التطوير معًا.

### حالة Docker بعد الترقية

- الحاويات الفعالة: 53 من أصل 57 حاوية موجودة.
- جميع الحاويات التي كانت تعمل سابقًا عادت للعمل.
- لم تظهر حالة `OOMKilled` ضمن الفحص السابق، ولم تظهر رسائل OOM في الإقلاع الجديد.
- ما زالت الحاويات دون حدود صريحة للذاكرة والمعالج وPIDs.

عدم وجود حدود لم يعد يمثل خطرًا فوريًا على RAM كما كان مع 16 GB، لكنه يبقى نقطة تنظيم مهمة؛ حاوية بها تسرب أو ارتفاع مفاجئ تستطيع استهلاك الذاكرة الإضافية كلها بمرور الوقت.

### ملاحظة مهمة حول `comm-worker`

في القياس الجديد كان `comm-worker` أكبر مستهلك بين الحاويات:

- RAM: نحو 2.56 GiB.
- CPU وقت العينة: نحو 10.7%.
- PIDs: نحو 523.

في الفحص السابق كانت أرقامه:

- RAM: نحو 1.19 GiB.
- CPU وقت العينة: نحو 7% إلى 8%.
- PIDs: نحو 483.

لا يمكن إثبات وجود memory leak من عينتين منفصلتين، خصوصًا لأن أحمال العمل قد تختلف. لكن وصول الحاوية إلى 2.56 GiB و523 PID بعد نحو 25 دقيقة من الإقلاع يجعل مراقبتها أولوية. السبب الظاهر ما زال تعدد جلسات Chromium وعمليات renderer التابعة لها.

يوصى بتسجيل القيم التالية دوريًا خلال عدة ساعات أو أيام:

```bash
docker stats --no-stream comm-worker
```

إذا استمر استهلاك RAM وعدد PIDs في الارتفاع دون أن يعودا إلى مستوى ثابت بعد انتهاء المهام، فهذا يقوي احتمال وجود جلسات Chromium لا تُغلق أو طوابير عمل غير محدودة أو تسرب موارد.

### مستهلكون آخرون بعد الترقية

- خادم تطوير Next.js الخاص بـ Lumina: نحو 1.03 GiB.
- `nest start --watch` الخاص بـ Lumina API: نحو 456 MiB.
- توجد عدة عمليات VS Code Extension Host بين نحو 500 و600 MiB للعملية الواحدة.
- توجد عدة عمليات Claude وTypeScript Server.
- ظهرت ثلاث عمليات Chromium renderer كبيرة، كل منها بين نحو 544 و620 MiB.

هذه الأحمال أصبحت ضمن قدرة السيرفر الجديدة، لكنها تفسر استخدام نحو 12 GiB حتى دون تنفيذ build كامل.

### التقييم بعد الترقية

الانتقال إلى 32 GB و12 نواة كان قرارًا مناسبًا لطريقة العمل الحالية. الوضع بعد الترقية أفضل بصورة واضحة:

- يوجد هامش RAM فعلي يقارب 19 GiB في وضع التشغيل الحالي.
- لا يوجد استخدام Swap.
- المعالج يملك هامشًا كبيرًا.
- لا يوجد ضغط ذاكرة أو I/O ملحوظ.
- خدمات الإنتاج وبيئة التطوير تعملان معًا دون ضغط ظاهر وقت الفحص.

من المتوقع أن يكون بناء Lumina وDocker أكثر استقرارًا بكثير. ومع ذلك، لا يثبت القياس الخامل وحده أن البناء لن يستهلك كامل الهامش؛ أفضل اختبار لاحق هو مراقبة الموارد أثناء عملية بناء فعلية.

### التوصيات بعد الترقية

1. الإبقاء على `COMPOSE_PARALLEL_LIMIT=1` مبدئيًا لأول بناء كامل، وقياس أعلى RAM مستخدمة خلاله.
2. إذا بقيت عدة GiB متاحة ولم يُستخدم Swap، يمكن تجربة توازٍ مقداره 2 لتقليل وقت البناء.
3. مراقبة `comm-worker` على مدى زمني، لأن استهلاكه الحالي أعلى بوضوح من القياس السابق.
4. عدم زيادة Swap حاليًا؛ 4 GB كافية كشبكة أمان مع 32 GB RAM، ولا يوجد مبرر لتغييرها الآن.
5. الإبقاء على `vm.swappiness=10`؛ الإعداد مناسب ولا يحتاج تعديلًا.
6. إضافة مراقبة تاريخية للموارد لاحقًا، لأن `sar` لم يكن مثبتًا أثناء الفحص، والقياسات التاريخية ستساعد على معرفة قمم البناء بدل الاعتماد على لقطة لحظية.
7. التفكير في حدود موارد مدروسة للحاويات بعد جمع بيانات فعلية، لا سيما حد PIDs وRAM لـ `comm-worker`.

### الخلاصة المحدثة

مشكلة نقص الموارد الأساسية حُلّت عمليًا بالترقية: أصبحت الذاكرة ضعف السابق، وزادت الأنوية من 8 إلى 12، واختفى استخدام Swap بعد الإقلاع الجديد. لا توجد حاجة حاليًا إلى خطة أعلى من هذه بناءً على القياسات المتاحة.

النقطة التي ما زالت تحتاج متابعة ليست سعة السيرفر، بل سلوك `comm-worker` وجلسات Chromium. كذلك يفضل تنفيذ أول بناء كامل بصورة متسلسلة مع مراقبة RAM وSwap، ثم زيادة التوازي تدريجيًا حسب النتائج.
