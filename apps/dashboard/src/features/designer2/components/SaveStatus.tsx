// Static placeholder — no autosave exists yet (designer.md Phase 9). Just the UI slot.
export function SaveStatus() {
  return <span className="text-xs text-gray-400 dark:text-gray-600">Not saved</span>;
}
